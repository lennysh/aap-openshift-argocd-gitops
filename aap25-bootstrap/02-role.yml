---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: argocd-aap25-manager
  namespace: aap25
  annotations:
    argocd.argoproj.io/sync-wave: "2"
rules:
  - apiGroups:
      - aap.ansible.com
    resources:
      - "*"
    verbs:
      - "*"
...
